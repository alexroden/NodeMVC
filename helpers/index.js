const path = require("path");

/*
 |-------------------------------------------------
 | Root of project.
 |-------------------------------------------------
 */
const __dirname = path.dirname(require.main.filename);

module.export = {
    __dirname
};